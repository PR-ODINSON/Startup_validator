import React from 'react';
import Dashboard from '../components/Dashboard';

const DashboardPage = () => {
  return (
    <main>
      <Dashboard />
    </main>
  );
};

export default DashboardPage;
// This is the main dashboard page component that imports and renders the Dashboard component.
// It serves as the entry point for the dashboard section of the application.